'use strict';

angular.module('tangHelper.minerService', [])
    .factory('operate', function($q, $timeout) {
        return function() {
            var df = $q.defer();
            $timeout(function() {
                df.resolve();
            }, 3000);
            return df.promise;
        };
    })