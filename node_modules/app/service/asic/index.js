'use strict';

var url = require('url');

angular.module('tangHelper.minerService', [])
    .factory('operate', function($q, $timeout) {
        return function(conf) {
            var df = $q.defer();
            return df.promise;


        };
    });