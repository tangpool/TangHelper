var app = angular.module('tangHelper')
    .service('getLocalIP', function($q) {
        var df = $q.defer();
        chrome.system.network.getNetworkInterfaces(function(items) {
            df.resolve(_.map(items, function(el) {
                return el.address;
            }));
        });
        return df.promise;
    });