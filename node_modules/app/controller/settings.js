var app = angular.module('tangHelper');

app.controller('settings', settings);

function settings() {
    this.config = {
        type: 'asic',
        pool1URL: 'us1.ghash.io',
        pool1Port: 3333,
        pool2URL: 'us1.ghash.io',
        pool2Port: 3334,
        username: 'my',
        prefix: null,

        mask: '255.255.255.0'
    };
}